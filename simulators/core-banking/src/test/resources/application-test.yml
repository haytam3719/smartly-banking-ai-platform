spring:
  datasource:
    url: jdbc:h2:mem:corebanking;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    locations: classpath:db/migration
internal-auth:
  dev-mode: true
simulator:
  faults:
    enabled: false
