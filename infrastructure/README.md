# Infrastructure

Platform Engineering owns local composition, observability defaults, messaging topology, and deployment templates. Runtime manifests will be added with real services rather than placeholders.

