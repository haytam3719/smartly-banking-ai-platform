# Domain agents

Each agent exposes only its declared canonical capabilities, validates typed arguments, applies domain safeguards, and returns structured evidence. Agents do not call one another or broaden their own permissions.

